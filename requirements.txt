flask>=2.3
requests>=2.31

# For wall/find_streams.py, which now runs here rather than on the Mac: it is
# what resolves the day's four YouTube video IDs for wall/streams.toml.
# Must be recent: 2026.03.17 silently reported every channel as offline,
# because YouTube changed the /streams tab and older extractors lose live_status.
yt-dlp>=2026.07.04

# Dev
pytest>=7.0
